import {
  helperCurrency
} from '../../../helpers/helper-currency';
import { module, test } from 'qunit';

module('HelperCurrencyHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = helperCurrency(42);
  assert.ok(result);
});
