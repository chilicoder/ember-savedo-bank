import {
  helperMoment
} from '../../../helpers/helper-moment';
import { module, test } from 'qunit';

module('HelperMomentHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = helperMoment(42);
  assert.ok(result);
});
